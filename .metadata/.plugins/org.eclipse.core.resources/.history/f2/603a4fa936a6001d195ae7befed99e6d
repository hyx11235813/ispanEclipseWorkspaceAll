package test;

import bean.User;
import userDao.UserDataAccessObject;

public class Test {

	public static void main(String[] args) {
		UserDataAccessObject userDao=new UserDataAccessObject();
		User user = userDao.findUserById(1);// 調用findUserByid方法,查詢的結果存在User中
		System.out.println(user.getId());//把user.setAccount，的user存儲的值拿來印出
		System.out.println(user.getAccount());
		System.out.println(user.getPwd());
		//.findUserByid(3)代表使用udao.java實作的方法並回傳一個三

		
		
		
		
		// insert資料
		
//		User user=new User();
//		user.setId(2);
//		user.setAccount("banana");
//		user.setPwd("papanana");
//		System.out.println(userDao.deleteUserById(2));
		
		
	}

}
